# Launching the Mobile App

To launch the mobile app on Windows using Flutter, navigate to the mobile directory and run the following command:

```
cd .\mobile\
flutter run -d windows
```

This will build and run the Flutter app on the Windows desktop target.
